name: Build, Package, and Release for Windows

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build-and-release:
    if: "!contains(github.event.head_commit.message, '[skip release]')"
    runs-on: ubuntu-latest

    env:
      DOTNET_VERSION: '10.0.x'
      RUNTIME_ID: 'win-x64'
      CONFIGURATION: 'Release'

    steps:
    # ---------------- checkout ----------------
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    # ---------------- read version ----------------
    - name: Read version from version.ini
      id: version
      run: |
        if [ -f version.ini ]; then
          VERSION=$(grep -oP '^\d+\.\d+\.\d+' version.ini)
        else
          VERSION="1.0.0"
        fi

        DATE=$(date +'%Y-%m-%d')

        echo "release_version=$VERSION" >> "$GITHUB_OUTPUT"
        echo "date=$DATE" >> "$GITHUB_OUTPUT"

    # ---------------- dotnet ----------------
    - name: Set up .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: Restore dependencies
      run: dotnet restore Updater.sln

    - name: Build
      run: dotnet build Updater.sln --configuration ${{ env.CONFIGURATION }}

    - name: Publish Console version
      run: |
        dotnet publish Updater/Updater.csproj \
          -c ${{ env.CONFIGURATION }} \
          -r ${{ env.RUNTIME_ID }} \
          --self-contained false \
          -p:PublishSingleFile=true \
          -o output/console

    - name: Publish GUI version
      run: |
        dotnet publish Updater.Console/Updater.Console.csproj \
          -c ${{ env.CONFIGURATION }} \
          -r ${{ env.RUNTIME_ID }} \
          --self-contained false \
          -p:PublishSingleFile=true \
          -o output/gui

    - name: Verify executables
      run: |
        test -f output/console/Updater.exe
        test -f output/gui/Updater.GUI.exe

    # ---------------- package ----------------
    - name: Generate SHA256 checksums
      run: |
        sha256sum output/console/Updater.exe > output/console/Updater.exe.sha256
        sha256sum output/gui/Updater.GUI.exe > output/gui/Updater.GUI.exe.sha256

    - name: Package Console version
      run: |
        sudo apt-get update
        sudo apt-get install -y p7zip-full
        cd output/console && 7z a "../../update_console_v${{ steps.version.outputs.release_version }}.zip" Updater.exe && cd ../..

    - name: Package GUI version
      run: |
        cd output/gui && 7z a "../../update_gui_v${{ steps.version.outputs.release_version }}.zip" Updater.GUI.exe && cd ../..

    - name: Generate update_info.json
      run: |
        VERSION="${{ steps.version.outputs.release_version }}"

        printf '{\n  "version": "%s",\n  "updateUrl": "https://github.com/%s/releases/download/v%s/update.zip"\n}\n' \
          "$VERSION" \
          "${{ github.repository }}" \
          "$VERSION" > update_info.json

    # ---------------- changelog ----------------
    - name: Generate changelog
      run: |
        git fetch --tags
        LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
        if [ -z "$LAST_TAG" ]; then
          git log --pretty=format:"- %s" > changelog.txt
        else
          git log "$LAST_TAG"..HEAD --pretty=format:"- %s" > changelog.txt
        fi

    # ---------------- release body ----------------
    - name: Generate release body
      run: |
        VERSION="${{ steps.version.outputs.release_version }}"
        DATE="${{ steps.version.outputs.date }}"

        echo "## 🚀 Updater v$VERSION" > release_body.md
        echo "" >> release_body.md
        echo "Updater 是一个 **用于 Windows 桌面程序的通用更新执行器**，用于在主程序退出后安全完成更新并重新启动程序。" >> release_body.md
        echo "" >> release_body.md
        echo "---" >> release_body.md
        echo "" >> release_body.md
        echo "### 📦 包含版本" >> release_body.md
        echo "" >> release_body.md
        echo "- **Updater.exe** - 控制台版本（适合后台运行）" >> release_body.md
        echo "- **Updater.GUI.exe** - 图形界面版本（适合用户交互）" >> release_body.md
        echo "" >> release_body.md
        echo "---" >> release_body.md
        echo "" >> release_body.md
        echo "### 🔧 核心能力" >> release_body.md
        echo "- 支持 ZIP / 安装包 / 增量更新模式" >> release_body.md
        echo "- 自动处理文件占用与权限问题" >> release_body.md
        echo "- 更新失败可追踪（日志输出）" >> release_body.md
        echo "- 适用于启动器 / 自动更新程序" >> release_body.md
        echo "- 支持测试模式（--test）进行功能验证" >> release_body.md
        echo "" >> release_body.md
        echo "---" >> release_body.md
        echo "" >> release_body.md
        echo "### 📦 构建信息" >> release_body.md
        echo "- 平台：Windows x64" >> release_body.md
        echo "- 运行时：.NET 10.0" >> release_body.md
        echo "- 构建方式：GitHub Actions 自动构建" >> release_body.md
        echo "- 发布日期：$DATE" >> release_body.md
        echo "" >> release_body.md
        echo "---" >> release_body.md
        echo "" >> release_body.md
        echo "### 📝 本次更新内容" >> release_body.md
        echo "" >> release_body.md
        cat changelog.txt >> release_body.md
        echo "" >> release_body.md
        echo "---" >> release_body.md
        echo "" >> release_body.md
        echo "🔐 所有发布文件均附带 SHA256 校验值" >> release_body.md
        echo "🤖 本版本由 GitHub Actions 自动构建并发布" >> release_body.md

    # ---------------- release ----------------
    - name: Create GitHub Release
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        VERSION="${{ steps.version.outputs.release_version }}"

        gh release create "v$VERSION" \
          "update_console_v$VERSION.zip" \
          "update_gui_v$VERSION.zip" \
          update_info.json \
          output/console/Updater.exe.sha256 \
          output/gui/Updater.GUI.exe.sha256 \
          --title "Release v$VERSION (Windows)" \
          --notes-file release_body.md

    # ---------------- bump version after release ----------------
    - name: Bump version.ini after release
      if: success()
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        OLD="${{ steps.version.outputs.release_version }}"
        IFS='.' read -r MAJOR MINOR PATCH <<< "$OLD"
        NEW="$MAJOR.MINOR.$((PATCH + 1))"

        echo "$NEW" > version.ini

        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add version.ini
        git commit -m "chore: bump version to $NEW [skip release]"
        git push
